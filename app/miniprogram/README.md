# MES 微信小程序

使用微信开发者工具直接导入本目录即可运行。

- 开发工具中需勾选“不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书”。
- 默认后端地址为 `http://127.0.0.1:8080`，可在 `utils/config.js` 修改。
- 真机调试时不能使用 `127.0.0.1`，应改为电脑局域网 IP，并确保手机可以访问后端端口。
- 发布前需要在微信公众平台配置 HTTPS request 合法域名，并替换 `project.config.json` 中的 AppID。
