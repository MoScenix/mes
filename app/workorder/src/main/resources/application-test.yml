spring:
  datasource:
    url: ${MYSQL_DSN:jdbc:mysql://127.0.0.1:3306/YOU_DB?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:YOUR_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: ${WORKORDER_METRICS_PORT:9998}

grpc:
  server:
    port: ${WORKORDER_GRPC_PORT:9092}
    advertise-host: ${CONSUL_SERVICE_HOST:127.0.0.1}

consul:
  address: ${CONSUL_ADDRESS:127.0.0.1:8500}

observability:
  prometheus:
    advertise-host: ${CONSUL_SERVICE_HOST:127.0.0.1}

management:
  otlp:
    tracing:
      endpoint: http://127.0.0.1:4318/v1/traces
